<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>${book.title}</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

    <div class="container d-flex justify-content-center align-items-center min-vh-100">
        <div class="card shadow-lg p-4" style="max-width: 500px;">
            <img src="${book.image?default('/images/book_placeholder.jpg')}" class="card-img-top rounded" alt="Обложка книги">
            <div class="card-body text-center">
                <h1 class="h4 fw-bold">${book.title}</h1>
                <p class="text-muted"><strong>Автор:</strong> ${book.author}</p>
                <p class="text-muted"><strong>Статус:</strong> ${book.status}</p>
                <a href="/" class="btn btn-primary mt-3">
                    ← на главную
                </a>
            </div>
        </div>
    </div>

</body>
</html>
